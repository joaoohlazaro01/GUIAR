name: Deploy Serv00

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: repo11.serv00.com
          username: GuiarTCCrotas
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/domains
            ls
